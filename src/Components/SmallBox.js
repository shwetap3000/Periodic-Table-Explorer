import './SmallBox.css';

const SmallBox = ({color}) => {
    return (
        <div 
        className="small-box" style={{backgroundColor: color}}>
        </div>
    );

}

export default SmallBox;
